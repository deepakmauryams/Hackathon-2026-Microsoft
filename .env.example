# Reference only: the app does not automatically load this file.
# Set RAG_API_KEY in your shell for local testing (at least 32 random characters).
RAG_API_KEY=
# Set after HTTPS setup so the OpenAPI servers URL is correct for Foundry.
PUBLIC_BASE_URL=